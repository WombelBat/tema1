#pragma once
#ifndef PLAYER_H
#define _PLAYER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

typedef struct PLAYER
{
    char *firstName;
    char *secondName;
    int points;
} PLAYER;

#pragma warning(pop)
#endif